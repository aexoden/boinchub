name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHONUNBUFFERED: "1"
  FORCE_COLOR: "1"

jobs:
  lint:
    name: Lint and Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@a0d2d53632934ae004f878c840055956d9f741b0 # v0.14.0

      - name: Set up uv
        run: devbox run -- uv sync --locked

      - name: Check formatting
        run: devbox run -- uv run ruff format --check

      - name: Lint
        run: devbox run -- uv run ruff check

      - name: Type check
        run: devbox run -- uv run mypy .

      - name: Install dependencies
        run: devbox run -- bash -c "cd frontend && pnpm install --frozen-lockfile"

      - name: Check TypeScript formatting
        run: devbox run -- bash -c "cd frontend && pnpm exec prettier --check ."

      - name: Lint TypeScript
        run: devbox run -- bash -c "cd frontend && pnpm exec eslint ."

      - name: Type check TypeScript
        run: devbox run -- bash -c "cd frontend && pnpm exec tsc --noEmit"
